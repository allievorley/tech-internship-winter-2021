let time = 0;

// Function called every second that gets Eric's timer. Will also have list of times to trigger to compare
// We probably need a stack or something with every 'event'
function TimerUpdate() {
    document.write("working");
}

// Function to display the activity
function Popup(activity) {

}

// Function to play the sound
function AlertSound() {

}